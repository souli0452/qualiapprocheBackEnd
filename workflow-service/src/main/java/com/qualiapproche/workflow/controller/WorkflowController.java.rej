@@ -62,4 +62,9 @@
         return ResponseEntity.ok(state);
     }
+
+    @PostMapping("/instances/states")
+    public ResponseEntity<Map<UUID, com.qualiapproche.workflow.dto.WorkflowStateDto>> getWorkflowStates(@RequestBody List<UUID> resourceIds) {
+        return ResponseEntity.ok(workflowService.getWorkflowStatesForResources(resourceIds));
+    }
 }
 
