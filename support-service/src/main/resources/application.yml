server:
  port: 8084

spring:
  application:
    name: support-service
  datasource:
    url: jdbc:postgresql://localhost:5433/qualiapproche_support
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/quali

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
