name: Deploy API Gateway

on:
  push:
    branches:
      - test

    paths:
      - 'api-gateway/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Vérifier le style de code (Checkstyle)
        run: mvn -B checkstyle:check -pl api-gateway -am

      - name: Build api-gateway
        run: mvn clean install -pl api-gateway -am -DskipTests -Dcheckstyle.skip=true

      - name: Trigger Coolify Deployment
        run: |
          echo "Calling Coolify webhook..."
      
          # Ajout du header Authorization avec le token
          response=$(curl -s -o response.txt -w "%{http_code}" \
          -X GET "${{ secrets.API_GATEWAY_WEBHOOK }}" \
          -H "Authorization: Bearer ${{ secrets.COOLIFY_TOKEN }}")
          echo "HTTP Status: $response"
          echo "Response:"
          cat response.txt
          if [ "$response" -ne 200 ]; then
            echo "Deployment webhook failed!"
            exit 1
          fi
          echo "Deployment triggered successfully!"