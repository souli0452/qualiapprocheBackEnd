package com.qualiapproche.utils;

/*import org.springframework.security.core.context.SecurityContextHolder;
import org.springframework.security.oauth2.jwt.Jwt;*/

public class UserConnect {
    /*public static OAuth2ResourceServerProperties.Jwt getCurrentUserToken() {
        return (OAuth2ResourceServerProperties.Jwt) SecurityContextHolder.getContext().getAuthentication().getPrincipal();
    }

    public static String getCurrentUsername() {
        return getCurrentUserToken().getClaim("preferred_username").toString();
    }

    public static String getUserFullname() {
        return getCurrentUserToken().getClaim("name").toString();
    }

    public static String getUserEmail() {
        return getCurrentUserToken().getClaim("email").toString();
    }

    public static String getCurrentUserId() {
        return getCurrentUserToken().getSubject();
    }

    public static String getCurrentUserEmail() {
        return getCurrentUserToken().getClaim("email").toString();
    }*/
}

